import { makeStyles } from "@material-ui/core";

export default makeStyles({
  body: {
    marginTop: 40,
    marginBottom: 40,
  },
  closeButton: {
    position: 'absolute',
    right: '1rem',
    top: '0.5rem',
    color: 'gray',
  }
});
